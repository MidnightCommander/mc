# Per-grammar config files and query files are installed by mc-ts-grammar.
# This directory is intentionally empty in the source tree.
if USE_INTERNAL_EDIT
syntaxtsdir = $(pkgdatadir)/syntax-ts
endif
